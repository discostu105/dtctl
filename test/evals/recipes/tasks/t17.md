What is the duration in MILLISECONDS of the single slowest ROOT span
(entry-point request) recorded over the LAST 24 HOURS, and which service
produced it? Report the true maximum duration, not a percentile — and be
careful that sampling does not hide the maximum.

ANSWER schema: {"duration_ms": <number>, "service": "<service name>"}
