How many log records did the service named "${EVAL_TRAP_SERVICE}" produce IN
TOTAL over the LAST 2 HOURS? Completeness matters: this service runs as
multiple instances; report a number you can defend as covering ALL of them.

ANSWER schema: {"count": <integer>}
