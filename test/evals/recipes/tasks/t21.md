How many Kubernetes pods CURRENTLY run the service named
"${EVAL_TRAP_SERVICE}"? The service is deployed in several environments —
count the pods behind ALL of its current deployments, and only pods (not
containers or other workload objects).

ANSWER schema: {"pods": <integer>}
