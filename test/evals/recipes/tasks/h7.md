Which log bucket currently holds the MOST log records in total storage (not
recent ingest — total records stored right now), and what is that bucket's
configured retention in days?

ANSWER schema: {"bucket": "<name>", "retention_days": <integer>}
