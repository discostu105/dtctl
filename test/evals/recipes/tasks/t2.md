Which service has the highest p95 span/request duration over the LAST 1 HOUR,
and what is that p95 in milliseconds? Consider ALL services that report spans,
not only those with an OTel service.name.

ANSWER schema: {"service": "<service name>", "p95_ms": <number>}
