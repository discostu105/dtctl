Which service has the highest p95 SPAN duration over the LAST 1 HOUR, and
what is that p95 in milliseconds? Consider ALL spans of ALL services that
report spans — including internal and client/outbound spans, NOT only
entry-point/root/request spans — and not only services with an OTel
service.name.

ANSWER schema: {"service": "<service name>", "p95_ms": <number>}
