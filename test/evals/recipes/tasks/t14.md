Log storage distribution: which retention BUCKET received the MOST log
records over the LAST 24 HOURS, and how many log records landed in it?

ANSWER schema: {"bucket": "<bucket name>", "count": <integer>}
