Davis problem load, over the LAST 7 DAYS:
(a) How many problems did Davis (Dynatrace's problem-detection engine) raise
    in TOTAL? Count DISTINCT problems, not state-update rows.
(b) How many of those problems are ACTIVE right now (latest known status)?

ANSWER schema: {"problems_7d": <integer>, "active_now": <integer>}
