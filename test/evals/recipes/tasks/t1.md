Where are errors coming from? Over the LAST 1 HOUR, identify the top source of
ERROR-level logs in this environment, at the granularity of the Kubernetes
container name, and its error-log count.

ANSWER schema: {"top_source": "<k8s container name>", "count": <integer>}
