Is there GenAI / LLM observability data in this environment? Identify the model
with the highest input-token consumption over the LAST 24 HOURS and its total
input tokens.

ANSWER schema: {"model": "<model id>", "input_tokens": <integer>}
