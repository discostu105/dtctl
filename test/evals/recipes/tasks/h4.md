Which monitored host currently has the LEAST free disk space, measured as
its average free-disk percentage over the LAST 1 HOUR? Report the host name
and that percentage.

ANSWER schema: {"host": "<name>", "free_percent": <number>}
