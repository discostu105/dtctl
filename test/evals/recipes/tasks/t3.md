Does this environment have Real User Monitoring (RUM) data? If yes, how many
RUM/user event RECORDS were captured in the LAST 24 HOURS? Count the stored
event records themselves, not a metric-derived approximation.

ANSWER schema: {"rum_present": <true|false>, "events_24h": <integer, 0 if none>}
