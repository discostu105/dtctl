Which Kubernetes container emitted the most WARN-level log records over the
LAST 6 HOURS, and how many? Consider only records attributed to a container.

ANSWER schema: {"container": "<name>", "count": <integer>}
