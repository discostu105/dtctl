From which country did the most real-user (RUM) events originate over the
LAST 24 HOURS? Ignore events whose country is unknown. Report the ISO
country code and the event count.

ANSWER schema: {"country": "<ISO code>", "events": <integer>}
