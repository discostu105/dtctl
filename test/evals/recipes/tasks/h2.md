Over the LAST 2 HOURS: how many DISTINCT traces were recorded in this
environment, and what is the average number of spans per trace?

ANSWER schema: {"traces": <integer>, "spans_per_trace": <number>}
